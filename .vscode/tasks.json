{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "nim compile --threads=on --debugger=native -d=debug --debugInfo=on --stackTrace=on --lineTrace=on --verbosity=1 main.nim",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}